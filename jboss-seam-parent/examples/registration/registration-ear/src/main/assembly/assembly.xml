<assembly
    xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">

    <id>exploded</id>
    <formats>
        <format>dir</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!-- include ear files-->
        <fileSet>
            <filtered>true</filtered>
            <directory>${project.build.directory}/${project.build.finalName}</directory>
            <outputDirectory>${project.build.finalName}.ear</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>
        <!-- include jar files-->
        <fileSet>
            <filtered>true</filtered>
            <directory>${basedir}/../${example.name}-ejb/target/classes</directory>
            <outputDirectory>${project.build.finalName}.ear/${example.name}-ejb.jar</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>
        <!-- include war files-->
        <fileSet>
            <filtered>true</filtered>
            <directory>${basedir}/../${example.name}-web/target/${example.name}-web-${project.version}</directory>
            <outputDirectory>${project.build.finalName}.ear/${example.name}-web.war</outputDirectory>
            <includes>
                <include>**/*</include>
            </includes>
        </fileSet>
    </fileSets>

</assembly>
